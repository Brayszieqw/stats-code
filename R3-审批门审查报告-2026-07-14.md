# Stats Code 审查报告 — R3 research-workflow 审批门

- **日期**：2026-07-14
- **方式**：Fable 5 主审定级 + Sonnet 5 双路取证（实现侧 / 测试侧）+ 主审定点抽查
- **范围**：只读审查
- **仓库**：`D:/stats code/repo`（ts-backend）

---

## 总结论（TL;DR）

整体严重度 **Medium**；**无 High+**；**不阻塞发布**。

门本体设计强：无旁路、无配置后门、fail-closed 全向、双 store CAS 逐字符一致。Medium 落在防御分支与聊天路径共 **11 项测试空白**；另有 **4 项 Low** + **3 项 Info**。

---

## 一、门的设计确认（正面结论）

**架构**：非「等待审批相位」状态机——无 Pending/Rejected 状态、无 reject 端点、无锁；靠 hash/版本绑定 + CAS + 执行前二次核验实现。协议只有 `Draft|Approved`（`domain.ts:182`），方案审批只有 `Approved` 字面量（`domain.ts:360`），审批记录原子追加、无中间态。

**绑定链**：
协议（Approved + approval_id） → 审计（`audit_sha256` 内容 hash 不含时间戳，`dataset-audit.ts:433-450`；`audit_rules_version` 入 hash 使规则升级自动作废旧审批） → 方案审批（绑协议版本 / 内容 hash / approval_id + 数据 sha + run_spec_sha + 审计记录） → execute 前六项全等复核（`research-workflow.ts:295-308`）+ 数据 / 审计重算（`:310-358`）+ 最终线性化复查（`:359-375`）。

**旁路穷尽**：`ResearchWorkflowService.execute` 全仓仅 4 调用点，全部过门——`router.ts:640`（audit）/ `:675`（approve）/ `:731`（run，要求显式 plan_id）、`orchestrator.ts:510`（聊天路径，`allowMatchingPlan` 按 spec-hash 反查仍受六项校验）。snapshot export 只读已有记录（`providers.ts:110-233`）；CLI replay 与审批体系零耦合（`engine/snapshot/replay.ts` 全文件无 SessionStore 引用）；orchestrator 无自动链式执行（`orchestrator.ts:505-537` 单次执行后 yield `choice_prompt`）。

**无后门**：`auto-approve` / `skip` / `bypass` 全仓 grep 未命中；`research-workflow.ts` 与 `router.ts` 中 `process.env` 零命中。

**CAS 一致性**：`mem-store.ts:90-96` 与 `file-session-store.ts:337-343` 五子句逐字符一致（Active + 协议 Approved + version/content_sha256/approval_id 三绑定）；`updateResearchProtocol` CAS 同样一致（`mem-store.ts:72` = `file-session-store.ts:320`）。

**竞态实测**：`gate.test.ts:237-290` 用门控 `readRawById` 制造 approve × 协议变更真竞态，断言 approve 409 落败且审批数组保持空——高质量用例。

---

## 二、问题清单（按严重度）

### M1 防御分支与聊天路径测试缺口 — 门的行为正确但近半防御分支无直接测试

- **严重度**：Medium
- **位置**：`research-workflow.ts:310-375`、`mem-store.ts:97`、`file-session-store.ts:337-343`、`orchestrator.ts:510`、`gate.test.ts` 全文件、`contract-schemas.test.ts` 全文件、`contract-golden.test.ts:58-217`
- **现象**：门的核心校验逻辑（execute 阶段的数据 / 审计重算、最终线性化复查、并发 approve、边界会话状态、聊天路径专属分支）均已在实现中正确实现 fail-closed，但对应分支缺少直接测试覆盖，回归时无法被测试报警。
- **证据**：见下方 11 项空白明细表。
- **何时出现**：这些分支只在竞态、数据/审计漂移、损坏路径触发，属于低频防御路径，正常用例测试无法覆盖。
- **建议改法**：纯测试轮补齐，零行为风险，无需改动实现代码。
- **测试**：详见下表 11 项空白。
- **置信度**：高
- **取证**：executor 双路（r3-impl / r3-tests）+ 主审抽查

**11 项测试空白明细：**

| # | 空白点 | 相关位置 | 现状 |
|---|--------|----------|------|
| 1 | execute 阶段数据集 sha 变更拒绝分支无测试 | `research-workflow.ts:310-319` | 无测试 |
| 2 | execute 阶段审计转 blocked 分支无测试 | `research-workflow.ts:331-339`（approve 阶段同逻辑有测 `gate.test.ts:292-316`） | 无测试（execute 侧） |
| 3 | execute 阶段审计 hash 漂移分支无测试 | `research-workflow.ts:340-347` | 无测试 |
| 4 | execute 阶段审批引用的审计记录缺失分支无测试 | `research-workflow.ts:348-358` | 无测试 |
| 5 | 最终线性化复查分支无直接命中测试 | `research-workflow.ts:359-375` | 无直接命中测试 |
| 6 | 双重 approve 同一 spec：两次都 201、产生两条独立等效记录，行为未被测试钉住 | `mem-store.ts:97` | 未测试钉住 |
| 7 | approve × approve 并发无测试（现有并发测试是 approve × 协议 PATCH） | `gate.test.ts:237-290` | 现有测试为不同并发场景 |
| 8 | 未知 session 调用 approve 无测试（其余端点有 404 测试，此端点无） | approve 端点 | 无测试 |
| 9 | 协议 Draft 状态直接 approve 无测试（防护在代码中已实现） | `file-session-store.ts:337-343` | 缺用例 |
| 10 | orchestrator 聊天路径（唯一 `allowMatchingPlan:true` 调用点）无专门 gate 测试——HTTP /run 用例测不到此分支 | `orchestrator.ts:510` | 无专门测试 |
| 11 | 同一 plan_id 真并发双发无测试（顺序复用改 args 被拒有测） | 对比 `gate.test.ts:175-198` | 真并发场景无测试 |

**关联缺口（并列小节）**：
- 重启加载时陈旧审批被过滤无专门测试（`file-session-store.ts:185-204`）。
- 契约测试仅存在性检查，approve/audit 无 golden 对拍（`contract-schemas.test.ts` 全文件仅路由注册 + schema 存在性；`contract-golden.test.ts:58-217` 无此两块）。

---

### L1 归档竞态缝隙

- **严重度**：Low
- **位置**：`mem-store.ts:102-108`、`file-session-store.ts:350-355`（对比 `appendAnalysisPlanApproval` 的五子句 CAS）
- **现象**：`appendSkillRun` 两个 store 均无 Active 检查，与 `appendAnalysisPlanApproval` 的五子句 CAS 形成对比。
- **证据**：`runner.run` 长跑期间会话被归档，跑完仍写入归档会话并刷新 `last_active_at`，与「已归档仅支持只读」（`research-workflow.ts:103`）字面矛盾。
- **何时出现**：长跑分析 + 并发归档。
- **建议改法**：`append` 侧加 Active 检查或写明豁免注释（`appendDatasetAudit` 同查）。
- **测试**：无
- **置信度**：高
- **取证**：主审定点 grep

---

### L2 加载期静默重建无日志

- **严重度**：Low
- **位置**：`file-session-store.ts:148-169`（协议哈希不符 / Approved 无 approval_id → 降级 Draft）、`file-session-store.ts:185-204`（审批逐条重校验不符 → 静默丢弃）
- **现象**：方向 fail-closed 正确，但零可观测。
- **证据**：sessions.json 被外部修改或损坏后，审批「凭空消失」无解释日志。
- **何时出现**：sessions.json 文件损坏或被外部修改后重启加载时。
- **建议改法**：丢弃 / 降级时写一行结构化日志。
- **测试**：无
- **置信度**：高
- **取证**：主审定点 grep

---

### L3 三路由校验顺序不一致

- **严重度**：Low
- **位置**：`router.ts:632,667`（audit/approve 先 schema 后 session，422 优先 404）、`router.ts:703-721`（run 相反，先 404/409 后 422）
- **现象**：同族端点错误优先级不统一。
- **证据**：audit/approve 与 run 三路由的 422/404 判定顺序不同。
- **何时出现**：请求同时命中 schema 校验失败与 session 不存在两种错误条件时。
- **建议改法**：统一约定（建议 422 先于 404）并写进契约测试。
- **测试**：无
- **置信度**：高
- **取证**：主审定点 grep

---

### L4 无单条方案撤销

- **严重度**：Low
- **位置**：`contract/routes.ts:108-262`（全量路由表确认无 reject 动作）、`router.ts:367-369`（PATCH 协议降 Draft 全局清场，approval_id 置 null）、`research-workflow.ts:301`（protocol_approval_id 不匹配即失效）
- **现象**：全仓无 reject 动作；作废已批方案只能 PATCH 协议降 Draft 全局清场。
- **证据**：旧审批记录不删除，但因 `protocol_approval_id` 不匹配而失效。
- **何时出现**：需要撤销单条已批准方案但不想影响协议整体状态时。
- **建议改法**：作为设计权衡可接受，但未文档化——建议在契约文档写明撤销语义。
- **测试**：无（结构性缺失，非测试缺口）
- **置信度**：高
- **取证**：主审定点 grep

---

### I1 store 所有权差异

- **严重度**：Info
- **位置**：`mem-store.ts:49-55,97`（get() 返回活引用不 clone）、`file-session-store.ts:307-309,344`（深拷贝）
- **现象**：两个 store 实现对返回对象的所有权处理不一致。
- **证据**：当前调用方只读未触发问题。
- **何时出现**：未来若有调用方对 get() 返回值做写操作。
- **建议改法**：属未来改动脆弱点，暂无需处理，留意即可。
- **测试**：无
- **置信度**：高
- **取证**：主审定点 grep

---

### I2 同步写事故性串行化

- **严重度**：Info
- **位置**：`file-session-store.ts:243-249`（save() = writeFileSync + renameSync）
- **现象**：mutation 阻塞整个 event loop。
- **证据**：比 mem-store 更强的意外串行化。
- **何时出现**：高并发写入场景。
- **建议改法**：性能而非正确性含义，暂无需处理。
- **测试**：无
- **置信度**：高
- **取证**：主审定点 grep

---

### I3 plan_id 非一次性凭证

- **严重度**：Info
- **位置**：execute 路径（`research-workflow.ts:272-434`，全程无「已消费」写操作）
- **现象**：execute 无「已消费」标记，同 spec 可重复执行。
- **证据**：对确定性统计引擎合理（重跑同方案）。
- **何时出现**：同一 plan_id 被多次用于 execute。
- **建议改法**：建议契约文档写明「审批授权的是方案，不是单次运行」。
- **测试**：无
- **置信度**：高
- **取证**：主审定点 grep

---

## 三、修复顺序建议

1. 补 M1 测试轮（纯测试，零行为风险）
2. L1 `appendSkillRun` / `appendDatasetAudit` 加 Active 检查
3. L2 `parseSessions` 结构化日志
4. L3 统一校验顺序 + 契约测试
5. L4 / I3 文档化撤销与审批语义

---

## 四、证据附录 A — 实现侧取证表（executor r3-impl）

**检查单 1 门的强制点**：
`requireApprovedProtocol` 缺失 / 非 Approved / 无 approval_id → 428，版本不符 → 409（`research-workflow.ts:107-125`，被 `:168`/`:276`/`:363` 调用；测试 `gate.test.ts:109-116`）。execute 核心门：planId 精确匹配或 `allowMatchingPlan` spec-hash 匹配，未命中 → 428（`:282-294`；`gate.test.ts:118-120`）。命中后六项全等校验 → 409（`:295-308`；`gate.test.ts:187-198`）。执行前数据 sha 重算 → 409（`:310-319`；无测试）。执行前审计重算 blocked → 先落盘再 409（`:320-339`；execute 阶段无专门用例）。审计 hash 不一致 / 审计记录缺失 → 409（`:340-358`；无测试）。final linearization check → 409（`:359-375`；无直接测试）。覆盖范围：audit/approve/execute 三阶段被门覆盖；protocol 起草、compile（review-only never persists，`router.ts:287`）、上传、会话管理、语音、llm-config、coverage-matrix、sidecar、snapshot-export 不执行分析不适用。

**检查单 2 状态机**：
`protocolStatus=['Draft','Approved']`（`domain.ts:182-183`）；`analysisPlanApproval.status=z.literal('Approved')`（`domain.ts:356-374`）。protocol.status 唯一写入点 PATCH /protocol（`router.ts:362-378` CAS）。审批唯一写入点：每次生成新 plan_id/approval_id 并 push，无去重（`research-workflow.ts:239-260`, `mem-store.ts:86-100`）。
- 2a 无等待相位：protocol Approved + 匹配非 blocked audit 即可随时 approve（`gate.test.ts:113-116`）。
- 2b 连续两次 approve：均 201、独立记录（无测试）。
- 2c 无 reject：最接近撤销 = PATCH protocol → Draft（`router.ts:367-369`）。
- 2d 降 Draft 后旧审批不删除但 execute 因 protocol_approval_id 不匹配 → 409（`state.ts:49-64` 接口无 remove；无测试）。

**检查单 3 绑定**：
approve 请求体 = skill_id/dataset_id/args/expected_protocol_version/expected_audit_id/expected_audit_sha256/audit_roles（`domain.ts:384-393`）。服务端校验 audit_id+audit_sha256 命中后再五项与新鲜重算 context 全等（`research-workflow.ts:212-223`）。协议内容编辑必然 version 自增（`router.ts:349-356`）。旧 expected_protocol_version 先拦截 409（`research-workflow.ts:116-123`；execute 侧有测 `gate.test.ts:202-235`，approve 自身无专门用例）。竞态直测 `gate.test.ts:237-290` 已覆盖。

**检查单 4 HTTP 契约**：
三路由 `router.ts:629-661`/`664-698`/`701-759`；无统一 gate 端点无 reject（`contract/routes.ts:108-262` 确认）。校验顺序：audit/approve 先 safeParse 后 session（422 优先 404，`router.ts:632,667`），run 相反（`:703-721`）。未知 session → 404 三路由一致（`router.ts:97-104,649-658,691-696,705-712`）。Archived → 409、无协议 → 428、无审批 → 428、过期 → 409（`research-workflow.ts:101-105`/`router.ts:713-715`）。`HTTP_STATUS_FOR` 与实现逐一一致（`domain.ts:469-488`）。

**检查单 5 旁路**：
grep `researchWorkflow\.(execute|auditDataset|approveAnalysisPlan)` 全 ts-backend 命中 = 4（`router.ts:640/675/731`、`orchestrator.ts:510`），全部过门。skillRunner/skillRegistry 仅声明零独立入口（`state.ts:230,232`）。snapshot export 只读 execute 后 `snapshotRunRecorder.register` 写入的记录（`router.ts:832-847`、`providers.ts:110-233`、`research-workflow.ts:416-428`）。CLI 拒绝执行（`engine/cli.ts:44-47,138-142`）；`replay.ts:1-207` 零 SessionStore 引用。orchestrator 单次执行后 yield `choice_prompt`（`orchestrator.ts:505-537`）。`appendAnalysisPlanApproval` 仅 2 实现、仅 `research-workflow.ts:261` 间接调用。

**检查单 6 跳过门配置**：
auto-approve/skip/bypass 全仓 grep 未命中（仅 cli.ts 的 launcher bypass 无关）；research-workflow.ts/router.ts 无 process.env。未找到任何开关。

**检查单 7 并发重入**：
store 方法体内无 await 天然原子，服务层跨 store 调用的 await 点之间可交错；无锁，靠 CAS（`mem-store.ts:67-76,86-100`）+ execute 派发前最后一道闸（`research-workflow.ts:359-375`）。`gate.test.ts:237-290` 是唯一并发测试（approve × 协议 PATCH）。门中步骤执行时 approve 到达：execute 通过闸后仅 `runner.run`（`:379` 不碰 store）与 `appendSkillRun`（`:408-415` 纯 push），并发 approve 独立走 CAS 互不干扰（推断，无直接测试）。plan_id 非一次性凭证：无「已消费」标记，同 plan_id 可重复执行；顺序复用改 args 被拒有测（`gate.test.ts:175-198`），真并发双发无测试。file-store 同步写 = 事故性全局串行化（`file-session-store.ts:243-249`）。

**补充 CAS 比对**：
`appendAnalysisPlanApproval` 五子句逐字符一致（`mem-store.ts:90-96` ≡ `file-session-store.ts:337-343`）；`updateResearchProtocol` 一致（`mem-store.ts:72` = `file-session-store.ts:320`）。差异：①mem-store `get()` 活引用 vs file-store 深拷贝；②file-store 加载期 `parseSessions` 二次校验（`:93-212`：协议哈希不符或 Approved 无 approval_id 降级 Draft `:148-169`；审批逐条校验不符丢弃 `:185-204`）；③file-store `save()` 同步阻塞写（`:243-249`）。

---

## 五、证据附录 B — 测试与持久化侧取证表（executor r3-tests）

**检查单 1 gate.test.ts 用例枚举（5 个 it）**：

- `:109-135` rejects unapproved runs：无协议 POST /run→428 ResearchProtocolRequired；有协议无计划→428 ResearchApprovalRequired；args 夹带伪造 `workflow_approval.plan_approved_at`→422 SkillInvalidArgs，runner 全程未调用
- `:137-200` audits, creates a server approval：PATCH /protocol→200(v1)；POST audit→200；POST approve→201（含 plan_id、run_spec_sha256）；POST /run 带 plan_id→200 runner 调用 1 次；同 plan_id 改 args 再 run→409 Stale，runner 仍 1 次
- `:202-235` invalidates an old plan when the protocol version changes：审批后 PATCH protocol v→2，旧 plan_id run→409，runner 未调用
- `:237-290` atomically refuses an approval when the protocol changes during audit recomputation：门控 readRawById 暂停批准中的二次哈希重算，并发 PATCH protocol(v→2)，释放后 pending approve→409，approvals 数组=[]
- `:292-321` persists audit blockers and refuses approval：伪造 audit_roles(primary_key:['x']) 触发 DUPLICATE_PRIMARY_KEY+AUDIT_ROLE_OVERRIDE_REJECTED 两 blocker；approve→409 ResearchAuditBlocked；runner 未调用；dataset_audits 长度 1（audit 本身落盘）

**检查单 2 持久化**：
协议相位整体作为 research_protocol 落盘，CAS 不符整体拒绝（`file-session-store.ts:318-325`；`research-protocol.test.ts:139-163` 乐观锁有测）。`appendAnalysisPlanApproval` 落盘前复查 Active+Approved+三绑定，不符返回 false 不落盘（`:334-348`；`gate.test.ts:237-290` 间接验证）。重启加载 `parseSessions` 对每条审批全量重算校验（版本/content_sha256/protocol_approval_id/dataset.sha256/audit_sha256/run_spec_sha256），不匹配静默过滤丢弃（`:185-204`；无专门测试）。类型系统无等待审批中间相位（`domain.ts:182,360`）——重启后协议停 Draft 或 Approved，审批要么有效要么为空，不存在「恢复为等待审批」。

**检查单 3 快照/重放**：
exporter.ts 无专门协议/审批字段，artifacts 不透明处理（`exporter.ts:63-88,150-276`）。审批状态经 server 层入快照：protocol.json/approval.json/dataset-audit.json 由运行时 record 拼出塞进 `workflow.steps[0].inputs` 与 artifacts（`providers.ts:164-215`、`state.ts:104-115`；`research-snapshot.test.ts:140-157` 验证 zip 内容）。replay 对这三个文件只做 SHA256 完整性比对，不解析语义不检查 status，无「执行」概念（`replay.ts:184-204,87-103`；happy path 有测 `research-snapshot.test.ts:158-160`，篡改路径无测试）。

**检查单 4 契约测试**：
`contract-schemas.test.ts` 仅路由注册（ids 含 approve_analysis_plan）+ schema 存在性（`:13,61-62`）；`HTTP_STATUS_FOR` 覆盖四个错误码（`:75-78`）；无 safeParse 样例用例。`contract-golden.test.ts` 有 health/session/llm-status/coverage-matrix/snapshot-export 的 golden 块（`:58-217`），无 audit/approve 块。

**检查单 5 空白清单 a–h**：
- a 相位不对 approve = 无测试（防护在 `file-session-store.ts:337-343`）
- b 双重 approve = 无测试
- c reject 后 approve = 结构不存在（无 reject 路由，`domain.ts:182,360`）
- d 协议变更旧审批失效 = 有测试（`gate.test.ts:202-235` + `:237-290`）
- e 并发 approve×approve = 无测试
- f 等待审批持久化恢复 = 该状态不存在（非未测试）
- g replay 对门 = happy path 有测、篡改无测
- h 未知 session approve = 无测试（approve 的 5 处测试引用全用有效 sid）

---

## 六、残余未知与后续

- engine 内 parity/replay 子命令实际 dispatch 调用者未定位（已确认 `replay.ts` 与审批体系零耦合，不影响 R3 结论）。
- `conversation-e2e.test.ts` 引用 approve 但未展开审读。
- **R4（replay 往返完整性）待审**，已有线索：replay 篡改路径（改 protocol.json/approval.json 字节应报 `input_artifact_sha256_mismatch`）无测试、happy path 仅 1 例（`research-snapshot.test.ts:158-160`）。
