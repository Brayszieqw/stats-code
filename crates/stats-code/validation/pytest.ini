[pytest]
# Make the `parity` package importable regardless of which directory pytest is
# invoked from (repo root or this validation dir). The path is resolved
# relative to this ini file, so it always points at
# crates/stats-code/validation/.
pythonpath = .
markers =
    slow: tests that take longer (skipped by default; use -m slow to run)
