// @stats-code/core — Session state machine, Skill registry, LLM client,
// validation, quota, store. Maps to the Rust `agent-core` crate.
// Depends on: @stats-code/server.

export const CORE_PACKAGE = '@stats-code/core' as const;
