// AUTO-GENERATED from packages/engine/src/sidecar/templates by scripts/embed-templates.mjs. Do not edit.
export const SIDECAR_TEMPLATES: Readonly<Record<string, string>> = {
  "anova\u0000Python": "import pandas as pd\nimport statsmodels.api as sm\nfrom statsmodels.formula.api import ols\nfrom statsmodels.stats.anova import anova_lm\n\ndata = pd.read_csv(\"data.csv\")\nmodel = ols(\"{{column.0.name}} ~ C({{column.1.name}})\", data=data).fit()\nresult = anova_lm(model, typ=2)\nprint(result)\n",
  "attributable_risk\u0000Python": "import numpy as np\nimport pandas as pd\n\ndata = pd.read_csv(\"data.csv\")\ntable = pd.crosstab(data[\"{{column.0.name}}\"], data[\"{{column.1.name}}\"]).to_numpy(dtype=float)\nrisk_exposed = table[1, 1] / table[1].sum()\nrisk_unexposed = table[0, 1] / table[0].sum()\nattributable_risk = float(risk_exposed - risk_unexposed)\nattributable_risk_percent = float((risk_exposed - risk_unexposed) / risk_exposed) if risk_exposed > 0 else float(\"nan\")\nprint(attributable_risk)\nprint(attributable_risk_percent)\n",
  "correlation\u0000Python": "import pandas as pd\nfrom scipy import stats\n\ndata = pd.read_csv(\"data.csv\")\nresult = stats.pearsonr(data[\"{{column.0.name}}\"], data[\"{{column.1.name}}\"])\nprint(result)\n",
  "cox\u0000Python": "import pandas as pd\nfrom lifelines import CoxPHFitter\n\ndata = pd.read_csv(\"data.csv\")\nfitter = CoxPHFitter()\nfitter.fit(data, duration_col=\"{{column.0.name}}\", event_col=\"{{column.1.name}}\")\nprint(fitter.summary)\n",
  "diagnostic_roc\u0000Python": "import pandas as pd\nfrom sklearn.metrics import roc_curve\n\ndata = pd.read_csv(\"data.csv\")\nfpr, tpr, thresholds = roc_curve(\n    y_true=data[\"{{column.0.name}}\"],\n    y_score=data[\"{{column.1.name}}\"],\n)\nprint(fpr)\nprint(tpr)\nprint(thresholds)\n",
  "kaplan_meier\u0000Python": "import pandas as pd\nfrom lifelines import KaplanMeierFitter\n\ndata = pd.read_csv(\"data.csv\")\nfitter = KaplanMeierFitter()\nfitter.fit(durations=data[\"{{column.0.name}}\"], event_observed=data[\"{{column.1.name}}\"])\nprint(fitter.survival_function_)\n",
  "life_table\u0000Python": "import pandas as pd\nfrom lifelines.utils import survival_table_from_events\n\ndata = pd.read_csv(\"data.csv\")\nresult = survival_table_from_events(\n    death_times=data[\"{{column.0.name}}\"],\n    event_observed=data[\"{{column.1.name}}\"],\n)\nprint(result)\n",
  "linear\u0000Python": "import pandas as pd\nimport statsmodels.api as sm\nfrom statsmodels.regression.linear_model import OLS\n\ndata = pd.read_csv(\"data.csv\")\ny = data[\"{{column.0.name}}\"]\nX = sm.add_constant(data[[\"{{column.1.name}}\"]])\nmodel = OLS(y, X).fit()\nprint(model.summary())\n",
  "logistic\u0000Python": "import pandas as pd\nimport statsmodels.api as sm\nfrom statsmodels.discrete.discrete_model import Logit\n\ndata = pd.read_csv(\"data.csv\")\ny = data[\"{{column.0.name}}\"]\nX = sm.add_constant(data[[\"{{column.1.name}}\"]])\nmodel = Logit(y, X).fit(disp=False)\nprint(model.summary())\n",
  "nonparametric\u0000Python": "import pandas as pd\nfrom scipy import stats\n\ndata = pd.read_csv(\"data.csv\")\ngroups = data[\"{{column.1.name}}\"].unique()\ngroup_a = data[data[\"{{column.1.name}}\"] == groups[0]][\"{{column.0.name}}\"]\ngroup_b = data[data[\"{{column.1.name}}\"] == groups[1]][\"{{column.0.name}}\"]\nresult = stats.mannwhitneyu(group_a, group_b)\nprint(result)\n",
  "or_rr\u0000Python": "import pandas as pd\nfrom scipy.stats import contingency\n\ndata = pd.read_csv(\"data.csv\")\ntable = pd.crosstab(data[\"{{column.0.name}}\"], data[\"{{column.1.name}}\"])\nresult = contingency.odds_ratio(table.to_numpy())\nprint(result.statistic)\nprint(result.confidence_interval())\n",
  "power_phase2\u0000Python": "import pandas as pd\nfrom scipy import stats\n\ndata = pd.read_csv(\"data.csv\")\n# columns referenced for parity context: {{column.0.name}}, {{column.1.name}}\np_null = 0.20\np_alt = 0.40\nalpha = 0.05\npower = 0.80\nn = 1\nwhile True:\n    critical = stats.binom.ppf(1 - alpha, n, p_null)\n    achieved_power = float(1 - stats.binom.cdf(critical, n, p_alt))\n    if achieved_power >= power:\n        break\n    n += 1\nprint({\"n\": n, \"critical\": float(critical), \"achieved_power\": achieved_power})\n",
  "power_phase3\u0000Python": "import pandas as pd\nfrom statsmodels.stats.power import TTestIndPower\n\ndata = pd.read_csv(\"data.csv\")\n# columns referenced for parity context: {{column.0.name}}, {{column.1.name}}\nanalysis = TTestIndPower()\nresult = analysis.solve_power(effect_size=0.5, alpha=0.05, power=0.80, ratio=1.0)\nprint(result)\n",
  "power_single_arm\u0000Python": "import pandas as pd\nfrom statsmodels.stats.proportion import proportions_ztest\n\ndata = pd.read_csv(\"data.csv\")\n# columns referenced for parity context: {{column.0.name}}, {{column.1.name}}\nsuccesses = int(data[\"{{column.0.name}}\"].sum())\ntotal = int(data[\"{{column.0.name}}\"].count())\nresult = proportions_ztest(count=successes, nobs=total, value=0.5)\nprint(result)\n",
  "standardization\u0000Python": "import numpy as np\nimport pandas as pd\n\ndata = pd.read_csv(\"data.csv\")\ncounts = np.asarray(data[\"{{column.0.name}}\"], dtype=float)\npopulations = np.asarray(data[\"{{column.1.name}}\"], dtype=float)\nstratum_rates = counts / populations\nweights = populations / populations.sum()\ndirect_standardized_rate = float(np.sum(stratum_rates * weights))\nprint(direct_standardized_rate)\n",
  "tableone\u0000Python": "import pandas as pd\nfrom scipy import stats\n\ndata = pd.read_csv(\"data.csv\")\ngroup = {{params.group_quoted}}\ncontinuous = [{{params.continuous_quoted}}]\ngroups = data[group].dropna().unique()\nfor variable in continuous:\n    group_a = data[data[group] == groups[0]][variable]\n    group_b = data[data[group] == groups[1]][variable]\n    result = stats.ttest_ind(group_a, group_b)\n    print(variable, result)\n",
  "ttest\u0000Python": "import pandas as pd\nfrom scipy import stats\n\ndata = pd.read_csv(\"data.csv\")\ngroups = data[\"{{column.1.name}}\"].unique()\ngroup_a = data[data[\"{{column.1.name}}\"] == groups[0]][\"{{column.0.name}}\"]\ngroup_b = data[data[\"{{column.1.name}}\"] == groups[1]][\"{{column.0.name}}\"]\nresult = stats.ttest_ind(group_a, group_b)\nprint(result)\n",
  "anova\u0000R": "data <- read.csv(\"data.csv\")\nfit <- stats::aov({{column.0.name}} ~ {{column.1.name}}, data = data)\nprint(summary(fit))\n",
  "attributable_risk\u0000R": "library(epiR)\ndata <- read.csv(\"data.csv\")\ntab <- table(data[[\"{{column.0.name}}\"]], data[[\"{{column.1.name}}\"]])\nresult <- epiR::epi.2by2(dat = tab, method = \"cohort.count\")\nprint(result)\n",
  "correlation\u0000R": "data <- read.csv(\"data.csv\")\nresult <- stats::cor.test(data[[\"{{column.0.name}}\"]], data[[\"{{column.1.name}}\"]], method = \"pearson\")\nprint(result)\n",
  "cox\u0000R": "library(survival)\ndata <- read.csv(\"data.csv\")\nfit <- survival::coxph(survival::Surv({{column.0.name}}, {{column.1.name}}) ~ 1, data = data)\nprint(summary(fit))\n",
  "diagnostic_roc\u0000R": "library(pROC)\ndata <- read.csv(\"data.csv\")\nresult <- pROC::roc(response = data[[\"{{column.0.name}}\"]], predictor = data[[\"{{column.1.name}}\"]])\nprint(result)\n",
  "kaplan_meier\u0000R": "library(survival)\ndata <- read.csv(\"data.csv\")\nfit <- survival::survfit(survival::Surv({{column.0.name}}, {{column.1.name}}) ~ 1, data = data)\nprint(summary(fit))\n",
  "life_table\u0000R": "library(KMsurv)\ndata <- read.csv(\"data.csv\")\nresult <- KMsurv::lifetab(\n  tis = data[[\"{{column.0.name}}\"]],\n  ninit = nrow(data),\n  nlost = data[[\"{{column.1.name}}\"]],\n  nevent = data[[\"{{column.1.name}}\"]]\n)\nprint(result)\n",
  "linear\u0000R": "data <- read.csv(\"data.csv\")\nfit <- stats::lm({{column.0.name}} ~ {{column.1.name}}, data = data)\nprint(summary(fit))\n",
  "logistic\u0000R": "data <- read.csv(\"data.csv\")\nfit <- stats::glm(\n  {{column.0.name}} ~ {{column.1.name}},\n  family = binomial(link = \"logit\"),\n  data = data\n)\nprint(summary(fit))\n",
  "nonparametric\u0000R": "data <- read.csv(\"data.csv\")\nresult <- stats::wilcox.test({{column.0.name}} ~ {{column.1.name}}, data = data)\nprint(result)\n",
  "or_rr\u0000R": "library(epitools)\ndata <- read.csv(\"data.csv\")\nresult <- epitools::oddsratio(data[[\"{{column.0.name}}\"]], data[[\"{{column.1.name}}\"]])\nprint(result)\n",
  "power_phase2\u0000R": "library(clinfun)\ndata <- read.csv(\"data.csv\")\n# columns referenced for parity context: {{column.0.name}}, {{column.1.name}}\nresult <- clinfun::ph2simon(pu = 0.20, pa = 0.40, ep1 = 0.05, ep2 = 0.20)\nprint(result)\n",
  "power_phase3\u0000R": "data <- read.csv(\"data.csv\")\n# columns referenced for parity context: {{column.0.name}}, {{column.1.name}}\nresult <- stats::power.t.test(n = NULL, delta = 0.5, sd = 1, sig.level = 0.05, power = 0.80)\nprint(result)\n",
  "power_single_arm\u0000R": "data <- read.csv(\"data.csv\")\n# columns referenced for parity context: {{column.0.name}}, {{column.1.name}}\nresult <- stats::power.prop.test(n = NULL, p1 = 0.5, p2 = 0.7, sig.level = 0.05, power = 0.80)\nprint(result)\n",
  "standardization\u0000R": "library(epitools)\ndata <- read.csv(\"data.csv\")\nresult <- epitools::ageadjust.direct(\n  count = data[[\"{{column.0.name}}\"]],\n  pop = data[[\"{{column.1.name}}\"]],\n  stdpop = data[[\"{{column.1.name}}\"]]\n)\nprint(result)\n",
  "tableone\u0000R": "library(tableone)\ndata <- read.csv(\"data.csv\")\nvars <- c({{params.continuous_quoted}})\nstrata <- {{params.group_quoted}}\nresult <- tableone::CreateTableOne(vars = vars, strata = strata, data = data)\nprint(result)\n",
  "ttest\u0000R": "data <- read.csv(\"data.csv\")\nresult <- stats::t.test({{column.0.name}} ~ {{column.1.name}}, data = data)\nprint(result)\n",
  "anova\u0000SAS": "proc import datafile=\"data.csv\" out=work.data dbms=csv replace; run;\n\nproc glm data=work.data;\n   class {{column.1.name}};\n   model {{column.0.name}} = {{column.1.name}};\nrun;\nquit;\n",
  "attributable_risk\u0000SAS": "proc import datafile=\"data.csv\" out=work.data dbms=csv replace; run;\n\nproc freq data=work.data;\n   tables {{column.0.name}}*{{column.1.name}} / riskdiff;\nrun;\n",
  "correlation\u0000SAS": "proc import datafile=\"data.csv\" out=work.data dbms=csv replace; run;\n\nproc corr data=work.data pearson;\n   var {{column.0.name}} {{column.1.name}};\nrun;\n",
  "cox\u0000SAS": "proc import datafile=\"data.csv\" out=work.data dbms=csv replace; run;\n\nproc phreg data=work.data;\n   model {{column.0.name}}*{{column.1.name}}(0) = ;\nrun;\n",
  "diagnostic_roc\u0000SAS": "proc import datafile=\"data.csv\" out=work.data dbms=csv replace; run;\n\nproc logistic data=work.data;\n   model {{column.0.name}}(event='1') = {{column.1.name}} / outroc=roc;\n   roc;\nrun;\n",
  "kaplan_meier\u0000SAS": "proc import datafile=\"data.csv\" out=work.data dbms=csv replace; run;\n\nproc lifetest data=work.data method=km;\n   time {{column.0.name}}*{{column.1.name}}(0);\nrun;\n",
  "life_table\u0000SAS": "proc import datafile=\"data.csv\" out=work.data dbms=csv replace; run;\n\nproc lifetest data=work.data method=life intervals=0 to 100 by 10;\n   time {{column.0.name}}*{{column.1.name}}(0);\nrun;\n",
  "linear\u0000SAS": "proc import datafile=\"data.csv\" out=work.data dbms=csv replace; run;\n\nproc reg data=work.data;\n   model {{column.0.name}} = {{column.1.name}};\nrun;\nquit;\n",
  "logistic\u0000SAS": "proc import datafile=\"data.csv\" out=work.data dbms=csv replace; run;\n\nproc logistic data=work.data;\n   model {{column.0.name}} = {{column.1.name}};\nrun;\n",
  "nonparametric\u0000SAS": "proc import datafile=\"data.csv\" out=work.data dbms=csv replace; run;\n\nproc npar1way data=work.data wilcoxon;\n   class {{column.1.name}};\n   var {{column.0.name}};\nrun;\n",
  "or_rr\u0000SAS": "proc import datafile=\"data.csv\" out=work.data dbms=csv replace; run;\n\nproc freq data=work.data;\n   tables {{column.0.name}}*{{column.1.name}} / cmh relrisk;\nrun;\n",
  "power_phase2\u0000SAS": "proc import datafile=\"data.csv\" out=work.data dbms=csv replace; run;\n/* columns referenced for parity context: {{column.0.name}}, {{column.1.name}} */\n\nproc power;\n   twosamplefreq test=pchi\n      groupproportions=(0.20 0.40)\n      alpha=0.05\n      power=0.80\n      ntotal=.;\nrun;\n",
  "power_phase3\u0000SAS": "proc import datafile=\"data.csv\" out=work.data dbms=csv replace; run;\n/* columns referenced for parity context: {{column.0.name}}, {{column.1.name}} */\n\nproc power;\n   twosamplemeans test=diff\n      meandiff=0.5\n      stddev=1\n      alpha=0.05\n      power=0.80\n      ntotal=.;\nrun;\n",
  "power_single_arm\u0000SAS": "proc import datafile=\"data.csv\" out=work.data dbms=csv replace; run;\n/* columns referenced for parity context: {{column.0.name}}, {{column.1.name}} */\n\nproc power;\n   onesamplefreq test=z method=normal\n      nullproportion=0.5\n      proportion=0.7\n      alpha=0.05\n      power=0.80\n      ntotal=.;\nrun;\n",
  "standardization\u0000SAS": "proc import datafile=\"data.csv\" out=work.data dbms=csv replace; run;\n\nproc stdrate data=work.data\n             refdata=work.data\n             method=direct\n             stat=rate(mult=100000);\n   population event={{column.0.name}} total={{column.1.name}};\n   reference  event={{column.0.name}} total={{column.1.name}};\nrun;\n",
  "tableone\u0000SAS": "proc import datafile=\"data.csv\" out=work.data dbms=csv replace; run;\n\nproc means data=work.data; var {{params.continuous_space}}; class {{params.group}}; run;\nproc freq data=work.data; tables {{params.group}}; run;\n",
  "ttest\u0000SAS": "proc import datafile=\"data.csv\" out=work.data dbms=csv replace; run;\n\nproc ttest data=work.data;\n   class {{column.1.name}};\n   var {{column.0.name}};\nrun;\n",
  "anova\u0000SPSS": "GET DATA /TYPE=TXT /FILE='data.csv' /DELIMITERS=',' /FIRSTCASE=2 /VARIABLES=ALL.\nDATASET NAME data WINDOW=FRONT.\nUNIANOVA {{column.0.name}} BY {{column.1.name}}.\n\n",
  "correlation\u0000SPSS": "GET DATA /TYPE=TXT /FILE='data.csv' /DELIMITERS=',' /FIRSTCASE=2 /VARIABLES=ALL.\nDATASET NAME data WINDOW=FRONT.\nCORRELATIONS VARIABLES={{column.0.name}} {{column.1.name}}.\n\n",
  "cox\u0000SPSS": "GET DATA /TYPE=TXT /FILE='data.csv' /DELIMITERS=',' /FIRSTCASE=2 /VARIABLES=ALL.\nDATASET NAME data WINDOW=FRONT.\nCOXREG {{column.0.name}} /STATUS={{column.1.name}}(1).\n\n",
  "diagnostic_roc\u0000SPSS": "GET DATA /TYPE=TXT /FILE='data.csv' /DELIMITERS=',' /FIRSTCASE=2 /VARIABLES=ALL.\nDATASET NAME data WINDOW=FRONT.\nROC {{column.1.name}} BY {{column.0.name}}(1) /PLOT=CURVE /PRINT=SE COORDINATES.\n\n",
  "kaplan_meier\u0000SPSS": "GET DATA /TYPE=TXT /FILE='data.csv' /DELIMITERS=',' /FIRSTCASE=2 /VARIABLES=ALL.\nDATASET NAME data WINDOW=FRONT.\nKM {{column.0.name}} /STATUS={{column.1.name}}(1).\n\n",
  "life_table\u0000SPSS": "GET DATA /TYPE=TXT /FILE='data.csv' /DELIMITERS=',' /FIRSTCASE=2 /VARIABLES=ALL.\nDATASET NAME data WINDOW=FRONT.\nSURVIVAL TABLES={{column.0.name}} /INTERVALS=THRU 100 BY 10 /STATUS={{column.1.name}}(1).\n\n",
  "linear\u0000SPSS": "GET DATA /TYPE=TXT /FILE='data.csv' /DELIMITERS=',' /FIRSTCASE=2 /VARIABLES=ALL.\nDATASET NAME data WINDOW=FRONT.\nREGRESSION /DEPENDENT={{column.0.name}} /METHOD=ENTER {{column.1.name}}.\n\n",
  "logistic\u0000SPSS": "GET DATA /TYPE=TXT /FILE='data.csv' /DELIMITERS=',' /FIRSTCASE=2 /VARIABLES=ALL.\nDATASET NAME data WINDOW=FRONT.\nLOGISTIC REGRESSION VARIABLES={{column.0.name}} /METHOD=ENTER {{column.1.name}}.\n\n",
  "nonparametric\u0000SPSS": "GET DATA /TYPE=TXT /FILE='data.csv' /DELIMITERS=',' /FIRSTCASE=2 /VARIABLES=ALL.\nDATASET NAME data WINDOW=FRONT.\nNPAR TESTS /M-W={{column.0.name}} BY {{column.1.name}}(0 1).\n\n",
  "or_rr\u0000SPSS": "GET DATA /TYPE=TXT /FILE='data.csv' /DELIMITERS=',' /FIRSTCASE=2 /VARIABLES=ALL.\nDATASET NAME data WINDOW=FRONT.\nCROSSTABS TABLES={{column.0.name}} BY {{column.1.name}} /STATISTICS=RISK CMH.\n\n",
  "tableone\u0000SPSS": "GET DATA /TYPE=TXT /FILE='data.csv' /DELIMITERS=',' /FIRSTCASE=2 /VARIABLES=ALL.\nDATASET NAME data WINDOW=FRONT.\nFREQUENCIES VARIABLES={{params.group}}.\nDESCRIPTIVES VARIABLES={{params.continuous_space}}.\n",
  "ttest\u0000SPSS": "GET DATA /TYPE=TXT /FILE='data.csv' /DELIMITERS=',' /FIRSTCASE=2 /VARIABLES=ALL.\nDATASET NAME data WINDOW=FRONT.\nT-TEST GROUPS={{column.1.name}}(0 1) /VARIABLES={{column.0.name}}.\n\n"
};
