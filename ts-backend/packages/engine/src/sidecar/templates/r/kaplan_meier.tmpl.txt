library(survival)
data <- read.csv("data.csv")
fit <- survival::survfit(survival::Surv({{params.time}}, {{params.event}}) ~ {{params.km_r_formula}}, data = data)
print(summary(fit))
{{params.km_r_logrank_block}}
