proc import datafile="data.csv" out=work.data dbms=csv replace; run;

proc phreg data=work.data;
   model {{params.time}}*{{params.event}}(0) = {{params.predictors_space}} / ties=efron;
   assess ph;
run;
