# Stats Code 只读工程审查 — R1(前端渲染面)+ R2(统计数值内核)

> 日期:2026-07-13 · 模式:纯工程只读审查 · 主会话模型:claude-fable-5
> 范围:本文件只覆盖 R1、R2 两轮;R1 = 前端富文本渲染注入面细化(承接上轮 item 10),R2 = 统计引擎逐算法数值正确性(推翻上轮 item 7 的「Info / 未见明确 bug」结论)。
> 产出方式:R1 与 R2 的 math+回归族由主会话 Fable 5 直读一手代码;R2 的检验族(ttest…tableone)由 Opus 4.8 子代理完成(Fable 子代理护栏误伤后按项目对策切换),主会话模型全程未切换。
> 证据口径:所有 `path:line` 均为实际读到的代码行;分类分「确认缺陷 / 推测 / 味道」。**本轮为纯静态审查,未运行测试套件**——所有标「确认缺陷」是对给定输入可从代码逻辑推证的错误,但修复后仍须补「命令→输出→PASS」验证门。

---

## 0. 一页速览(按修复优先级)

| # | 轮次 | 严重度 | 短名 | 位置 | 一句话 |
|---|------|--------|------|------|--------|
| R2-A | R2 | **High** | 非有限统计量映射 p=1 → 极性反转 | `math/distributions.ts:41,49` | `F=+Inf`/`t=+Inf`(无穷显著)被吞成 p=1(最不显著),4 个模块中招 |
| R2-C | R2 | 中高 | epi 2×2 零单元格无校正 → CI 出 NaN/Inf | `stats/epi.ts:27-42` | 任一格为 0 直接 1/0,OR/RR 置信区间外泄 NaN/Inf |
| R2-D | R2 | Medium | Kruskal-Wallis 缺结修正 | `stats/nonparametric.ts:85-93` | H 未除结修正因子,同文件 MWU 却做了 → 遗漏;有结时 p 偏保守 |
| R2-E | R2 | Medium | power phase3 迭代未达标返回不配对 | `stats/power.ts:140-152` | 100 步未收敛时 requiredN 与 achievedPower 字段不对应且无标记 |
| R2-B | R2 | Medium | 退化输入下回归族 p 极性互相矛盾 | `stats/logistic.ts:137`, `stats/cox.ts:447` | 同是 se=0,linear 报 p=1、logistic/cox 报 p=0,契约不一致 |
| R2-F | R2 | Medium | 近奇异设计静默 ridge,SE 被偏置无标记 | `math/linalg.ts:105-124` | ridge 生效后无标记返回,logistic/cox 的 SE 静默失真 |
| R1 | R1 | Medium | 图表 tooltip 富文本越过 React 转义 | `web/src/components/StatsChartRenderer.tsx:76,172,304` | formatter 把数据字段拼进 HTML 串,默认 html 模式解释标记;测试因 mock ECharts 全盲 |
| R2-G | R2 | Low | logistic 空矩阵抛 TypeError 而非契约错 | `stats/logistic.ts:127` | `x[0]!.length` 在 n===0 检查之前执行 |

> 「味道」类与良好实践在各节末尾单列,不进本表。

---

## R1 — 前端富文本渲染注入面

### 结论先行
前端 XSS 主注入面**基本干净**,但存在**唯一一个逃逸出 React 自动转义的富文本渲染点**:ECharts 图表 tooltip 的 `formatter` 返回串。上一轮把 web 定为 Low 并写「未见明显 XSS(React 转义)」——该结论对 JSX 文本节点成立,但 React 转义**管不到 ECharts 组件内部的 DOM 写入**,真正的口子在这里。

### 已排除的注入面(一手证据)
- `web/src` 全域 **零** `dangerouslySetInnerHTML` / `innerHTML` / `v-html` / `insertAdjacentHTML` / `outerHTML` / `document.write`(全文 grep 命中 0)。
- **无**任何第三方 markdown / 高亮 / SVG 渲染库(`marked`/`markdown-it`/`react-markdown`/`remark`/`rehype`/`highlight`/`prismjs`/`shiki`/`katex`/`dompurify` 在 `web/package.json` 均无)。
- 等价代码侧栏 `EquivalentCodeSidecar.tsx:291-296` 用 `<pre className="sidecar-snippet"><code>{snippetText}</code></pre>` —— JSX 文本节点,React 自动转义,**安全**。
- 技术栈 React 19。

### 唯一逃逸点(确认 sink)
- **严重度**: Medium
- **位置**:
  - `web/src/components/StatsChartRenderer.tsx:76-80`(Kaplan-Meier tooltip)
  - `:172-175`(回归森林图 tooltip)
  - `:304-306`(ANOVA 组间对比 tooltip)
  - 全文件**无 `renderMode` 设置** → ECharts tooltip 默认 `'html'` 模式(已 grep 确认无 `renderMode`)。
- **现象**:三处 tooltip `formatter` 返回值含 `<br/>`/`<b>` 标记并用模板串插值文本字段;html 模式下 ECharts 把该串当 DOM 标记解释。若某文本字段的值含标记字符,tooltip 会按标记解释而非原样显示。
- **证据(字段来源契约)**:
  - `:78` `${p.seriesName}`、`:172` `<b>${coeff.term}</b>`、`:304` `<b>${names[idx]}</b>` —— 三个插值都是**字符串**字段。
  - `seriesName` / `names[idx]` = `payload.groups[]`(数据集分组列取值);`coeff.term` = 回归自变量名(= 上传 CSV 的列名/表头)。**均可回溯到用户上传数据的文本内容**。
  - **转义边界**:React 自动转义只作用于 JSX 文本节点;ECharts tooltip formatter 返回串走组件内部 DOM 写入,**不经 React**。这是全前端 React 转义唯一管不到的地方;数值字段(p 值、均值)不参与,风险只在文本字段。
- **何时出现**:数据集某分组列的值、或某回归自变量列名含标记字符 → 完成一次生存/回归/方差分析 → 用户悬浮对应图表 tooltip。医疗场景「打开他人导出的 CSV → hover 图表」是现实路径。叠加 R2 无关,但叠加上一轮 [4] CORS `*` 会放大后果面。
- **建议改法(二选一,最小方向)**:
  - (a) 给这三处 tooltip 配置加 `renderMode: 'richText'`(canvas 绘制,不解释标记);或
  - (b) 在 formatter 内对**文本字段**(seriesName / term / names)包一个 HTML 实体转义函数 `esc()`,数值字段免。
  - 推荐 (b):改动集中在 formatter 内三行,不改渲染器行为,风险最低。
- **测试缺口(确认=高)**:`web/src/components/StatsChartRenderer.test.tsx:6` `vi.mock('echarts-for-react', …)` 把整个 ECharts **mock 掉** → tooltip formatter 的真实 DOM 渲染/转义行为**从未被断言**。无「含标记字符的分组名/列名 → tooltip 输出被转义」用例。
- **置信度**:sink 确认=**高**(formatter 返回 HTML + 默认 html 模式,均一手证实);端到端严重度=**中**(取决于后端是否清洗列名/分组值,前端审查不可见该上游契约)。

### R1 待补充核实(下一步可选)
- 后端 dataset ingest 层是否对列名/分组值做过字符白名单(若已清洗,R1 端到端严重度可降到 Low)。这是前端审查照不到的上游契约,需读 `packages/server` 的 dataset 解析路径确认。

---

## R2 — 统计引擎逐算法数值核对

### 结论先行
上一轮 item 7 结论「结构标准、未见明确 bug、Info 级」**推翻**。存在多个**对具体输入可从代码推证**的数值缺陷:p 值极性反转、NaN/Inf 进入响应、结修正遗漏、迭代边界字段错配。且这些缺陷**几乎全部落在 parity 基线照不到的退化输入区**(现有基线用的都是良态数据集)——数值正确性缺口与 parity 盲区高度重叠。

---

### R2-A 【确认缺陷 · High】非有限统计量一律映射 p=1 → 「无穷显著」被报成「最不显著」

**根因**
- `math/distributions.ts:41` `if (f <= 0 || !Number.isFinite(f)) return 1;`(fDistributionPValue)
- `math/distributions.ts:49` `if (df <= 0 || !Number.isFinite(t)) return 1;`(tDistributionPValue)

把 `+Inf`(合法的「无穷显著」,应 p≈0)和 `NaN` 一起吞成 p=1(最不显著)。

**中招点(4 处)**
| 模块 | 位置 | 触发输入 | 现状 | 应为 |
|------|------|----------|------|------|
| ANOVA | `stats/anova.ts:53-54` | `oneWayAnova([[5,5,5],[7,7,7]])`(组内常数、组间不同) → ssWithin=0 → F=+Inf | **p=1** | p≈0 |
| 相关 | `stats/correlation.ts:75-76` | `pearsonCorrelation([1,2,3,4],[1,2,3,4])` → r=1 → denom=0 → t=+Inf | **p=1** | p≈0 |
| 相关(二级) | `stats/correlation.ts:75` | r=-1 时 t 仍为 `+POSITIVE_INFINITY` | **t 符号丢失** | t=-Inf |
| 线性回归 | `stats/linear.ts:70-71` | y 与 X 精确线性(如 y=2x+1)→ ssRes=0 → 各系数 stdError=0 → t=+Inf | **每系数 p=1** | p≈0 |
| 线性回归(模型) | `stats/linear.ts:89-90` | 同上 → msRes=0 → F=+Inf | **模型 p=1** | p≈0 |

> 讽刺细节:correlation 若**去掉** guard,`x=df/(df+Inf)=0` 反而算出 p=0(正确);**guard 本身是唯一致错原因**。ANOVA 去掉 guard 则 `x=(df1·Inf)/(df1·Inf+df2)=NaN` → p=NaN,仍需显式处理。

**现象(用户可见)**:上传「太干净」的数据(常数对照组 / 精确线性关系)→ 结果面板显示 `F=Infinity` 却 `p=1.000`,结论方向完全颠倒。医疗统计属实质错误。

**修复路线**
1. `distributions.ts` 两处 guard 拆分极性:
   - `fDistributionPValue`:`if (!Number.isFinite(f)) { return Number.isNaN(f) ? <抛错或NaN标记> : (f > 0 ? 0 : 1); }`,再处理 `f<=0 → 1`。
   - `tDistributionPValue`:`if (!Number.isFinite(t)) { return Number.isNaN(t) ? <标记> : 0; }`(t 双侧,±Inf 都 →0)。
2. `correlation.ts` t 统计量符号:把 `Number.POSITIVE_INFINITY` 改为 `Math.sign(r) * Number.POSITIVE_INFINITY`(r=0 的退化已在上游 denom 分支处理)。
3. 决定 NaN 的契约:是抛错(响亮失败)还是返回带 `degenerate: true` 标记的结果——需与 R2-B 一起统一(见下)。
**测试**:补三个退化用例——「组内常数的 ANOVA」「完美相关」「完美拟合 OLS」;断言 p→0 且不出现 NaN。

---

### R2-B 【确认缺陷 · Medium】退化输入下回归族 p 值极性互相矛盾

- `stats/logistic.ts:137-138`:se=0 → `z=+Inf` → `2*(1-normalCdf(Inf))` = **p=0**
- `stats/cox.ts:447-448`:se=0 → `normalCdfAbramowitz(Inf)=1` → **p=0**
- 对照 R2-A:同是「se=0」退化,linear/anova/correlation 报 **p=1**,logistic/cox 报 **p=0**;两边都无任何 degenerate 标记。

**问题**:同一类退化(标准误为 0),不同模块给出相反的 p 值极性,且都不告诉调用方「这是退化」。契约不一致。
**修复路线**:与 R2-A 合并处理——全族统一「se=0 / 统计量非有限」的语义:要么统一 p→0 且加 `degenerate: true`,要么统一抛契约错误。推荐前者(不中断整份分析报告),并让该标记透传到 API 响应(参见 R2-F 已有的 `converged` 透传链)。

---

### R2-C 【确认缺陷 · 中高】epi.ts 2×2 零单元格无 Haldane-Anscombe 0.5 校正 → CI 产出 NaN/Inf

- `stats/epi.ts:27-42`:入口只挡负数(`some(v => v < 0) → throw`),**不挡 0**。
  - `oddsRatio = (a*d)/(b*c)`;`seLogOr = sqrt(1/a+1/b+1/c+1/d)`;CI = `exp(lnOr ± z·seLogOr)`。
  - a=0 → OR=0, lnOr=-Inf, seLogOr=Inf → `orCiUpper = exp(-Inf + Inf) = exp(NaN) = **NaN**`。
  - b=0 或 c=0 → OR=+Inf, seLogOr=Inf → CI 两端 **NaN/Inf**。
  - RR:c=0 → riskUnexposed=0 → RR=+Inf;a=b=0 → riskExposed=0/0=**NaN** 向下传播。
- 同族 `attributableRisk`(`epi.ts:76-83`):`a+b=0` / `c+d=0` / `total=0` 无守卫(只挡负数)→ risk=NaN/Inf 传播;`attributableRiskPercent = rr>0 ? … : 0` 中 `NaN>0` 为 false → **静默返回 0**,掩盖问题。
- 触发:`orRr(0,10,5,10)` → OR=0, orCiUpper=**NaN**;`orRr(5,0,5,10)` → OR=Inf, CI 全 NaN。

**修复路线**:
1. 检测任一单元格为 0 → 加 0.5 连续性校正(Haldane-Anscombe)后再算,或返回显式 `not_computed`(与 contingency 的 `zero_margin` 风格一致)。
2. 禁止 NaN/Inf 进入响应契约:出口统一校验 `Number.isFinite`,非有限则改标记。
3. `attributableRiskPercent` 的 `rr>0` 分支要显式区分 `Number.isNaN(rr)`,不能把 NaN 静默折成 0。
**测试**:补「含 0 单元格的 2×2」用例,断言要么校正后有限、要么 not_computed,绝不 NaN。

---

### R2-D 【确认缺陷 · Medium】Kruskal-Wallis 缺结(ties)修正

- `stats/nonparametric.ts:85-93`:`h *= 12 / (n*(n+1))` —— 之后**没有**再除以结修正因子 `1 - Σ(tᵢ³-tᵢ)/(n³-n)`。
- **遗漏而非设计的铁证**:同文件 Mann-Whitney(`nonparametric.ts:42-46`)**实现了**结修正(`tieSum += t**3 - t; varU = …·(n+1 - tieSum/(n(n-1)))`),KW 却漏。
- 后果:有结(大量并列值)时 H 偏小、p 偏大(过度保守)。触发:`kruskalWallis([[1,1,2],[2,3,3]])`。
- 附带味道:`nonparametric.ts:95` 用 `1 - chiSquareCdf(h, df)` 且**未 clamp**,而项目已有 clamp 的 `chiSquareP` 未复用;大 H 远尾有 1-CDF 抵消损失。

**修复路线**:算出 `C = 1 - Σ(tᵢ³-tᵢ)/(n³-n)`(tᵢ 为各并列块大小),`h /= C`(C>0 时);p 值改用 `chiSquareP(h, df)` 复用 clamp 与上尾。
**测试**:KW 恰是 parity 未断言模块(基线只断言 MWU)——补一个有结数据集的 KW 基线。

---

### R2-E 【确认缺陷 · Medium】power.ts phase3 迭代未达标时返回字段不配对

- `stats/power.ts:140-152`:
  - `let requiredN = seed; let achievedPower = 0;`
  - `for (n = seed; n <= seed+100; n++) { … achievedPower = 1 - noncentralTCdf(…); if (achievedPower >= power) { requiredN = n; break; } }`
- 若 100 步内功效未达标(target→1 / effectSize→0,功效曲线近平)→ 不 break → **返回 `requiredN=seed`(初值),而 `achievedPower` 是 n=seed+100 处的值** → 两字段不对应;且 `achievedPower < power`(未达标)却当正常返回,**无告警**。
- 非无限循环(步数硬上限 100),但产出静默错误配对。触发:`powerPhase3(0.05, 1.0, 0.05, 0.999)` 类。
- 对照:`powerSingleArm`/`powerPhase2` 为闭式 `Math.ceil`,必终止无此问题。

**修复路线**:循环结束后判断是否命中 break;未命中则返回 `{ requiredN: null 或 seed+100, achievedPower, converged: false }` 显式标记搜索未达标,禁止 requiredN 与 achievedPower 错配。可选扩大搜索窗或改二分。
**测试**:补「高目标功效 + 中小效应」用例,断言未达标时字段自洽且有 converged=false。

---

### R2-F 【确认隐患 · Medium】近奇异设计下静默 ridge 正则化,SE 被偏置且无标记

- `math/linalg.ts:105-124` `invertMatrixWithRidge`:裸 `invertMatrix` 失败后,ridge 从 `1e-8` 每次 ×10 试到 8 次(至 1e-1),成功即返回,**无任何标记**。
- 调用链:logistic 的步进与协方差(`logistic.ts:87,133`)、cox 的步进与协方差(`cox.ts:267,282`)全走它 → **SE 直接来自被 ridge 偏置的协方差**。
- 契约不一致:linear(`linear.ts:48`)用裸 `invertMatrix`,共线**直接抛错**(响亮失败);logistic/cox 却静默改数。
- 已确认的缓解:`server/src/conversation/skill-runner.ts:220` 对 `!converged || maxAbsBeta>10 || maxStdError>10` 出 warning——但 ridge 生效且量级温和时无任何信号。

**修复路线**:`invertMatrixWithRidge` 返回 `{ inverse, ridgeApplied: boolean, ridgeValue: number }`;fit 结果加 `regularized` 字段并透传到诊断,让用户知道 SE 被正则化偏置。
**测试**:补一个近共线设计矩阵用例,断言 `regularized=true` 被标记且透传。

---

### R2-G 【确认缺陷 · Low】logisticRegression 空矩阵抛 TypeError 而非契约错误

- `stats/logistic.ts:127` `const p = x[0]!.length;` —— 在 `fitLogistic` 的 `n===0` 检查**之前**执行。
- `logisticRegression([], [])` 抛 `Cannot read properties of undefined (reading 'length')`,而非设计好的 `'Empty design matrix.'`。
- **修复路线**:`logisticRegression` 顶部先 `if (x.length === 0) throw new Error('Empty design matrix.')`,再取 p。

---

### R2 味道(简列,能用但脆弱)
- **NR 无步长控制**:`logistic.ts:92` / `cox.ts:272` 裸 Newton 步,无阻尼/线搜索;有 50 次上限 + converged 标记传播,风险有界。
- **A&S 正态近似三处重复**:`distributions.ts` / `cox.ts:119`(normalCdfAbramowitz)/ `correlation.ts:42-57`(内嵌 normQuantile,精度仅 ~4.5e-4,拖低 Fisher-z CI);项目已有高精度 `inverseNormal`(Acklam ~1e-9)与 `normalCdf` 未被复用。
- **绝对阈值不随尺度缩放**:`linalg.ts:81` 选主元奇异判据 `1e-15` 是绝对值;`linalg.ts:143` 行列式同款;病态但未奇异的矩阵可能漏检。
- **迭代上限静默**:`linalg.ts:259` jacobiEigh 200 sweep、`special.ts` 连分数上限(300/200)达到上限均静默返回末值,不标记降精。
- **p 值下界不 clamp**:logistic/cox 的 `2*(1-Φ(|z|))` 在 |z|≳8 下溢为 0,近似误差理论上可产生 ~-1e-7 的负 p;无 clamp 下界。
- **排序比较器遇 NaN**:`rank.ts:7`、`diagnostic.ts:33,77` 的 `sort((a,b)=>a.v-b.v)` 遇 NaN 顺序未定义;依赖上游删除缺失值,本层无防御。
- **KW 结修正的 clamp 缺失**(已并入 R2-D)。

### R2 良好实践(与缺陷同权重记录,修复时勿破坏)
- `special.ts:12-18` sigmoid 两分支稳定,无 exp 溢出。
- `cox.ts:178` risk-score 减 maxEta 平移防 exp 溢出,logPL 再加回。
- `distributions.ts:43-52` t/F p 值直接走不完全 Beta 上尾 + clamp01,远尾无 1-CDF 抵消。
- `linalg.ts:59-102` invertMatrix 选主元(partial pivoting)+ 奇异抛错;`:173-196` cholesky 非正定抛错。
- `linear.ts:40-43` n≤p 抛错、两遍 SS;`logistic.ts:56` 完全分类(单一类别)校验、概率钳制 [1e-9, 1-1e-9]。
- `cox.ts:112,307` 全删失抛错、PH 检验返回显式 `not_computed` + reason;Efron 结处理对齐 lifelines/R。
- **三个模块可作全库样板**:contingency(log-sum-exp Fisher、稀疏走精确/否则 not_computed)、survival(Greenwood 除零守卫、degenerate_variance 路径)、tableone(SMD 退化返 null 而非 NaN)。

---

### R2 Parity 覆盖对照(缺陷与盲区高度重叠)

| 模块 | 基线目录 | 测试中实际断言 | 盲区 / 与缺陷关系 |
|------|----------|----------------|-------------------|
| linear | SAS+SPSS | ✅ 系数/SE/R²/F(batch-b) | 退化路径(R2-A)无覆盖 |
| logistic | SAS+SPSS | ✅ β/OR/SE/logL(iterative) | se=0、完全分离、加权路径无覆盖 |
| cox | SAS+SPSS | ✅ β/HR/SE/logPL(iterative) | 同上 + 加权 Efron |
| ttest | SAS+SPSS | ⚠️ 仅 pooled-t(在 tableone 基线下) | **Welch/paired/one-sample 生产函数无 oracle** |
| anova | SAS+SPSS | ✅ SS/df/F | R2-A 的 F=Inf 路径不被触发 |
| contingency | fisher fixture | ⚠️ Fisher fixture 未被 loadBaseline 消费 | **Pearson χ² 无任何基线** |
| correlation | SAS+SPSS | ✅ pearson_r + spearman_rho | R2-A 的完美相关路径无覆盖 |
| nonparametric | SAS+SPSS | ⚠️ 仅 Mann-Whitney U | **Kruskal-Wallis 未断言(R2-D 恰落此)** |
| survival(KM) | SAS+SPSS | ✅ 仅 KM 断言 | **log-rank 完全无基线/无引用 → 最高危盲区**;life_table 基线孤立 |
| power | SAS | ✅ single_arm/phase2/phase3 | 仅 SAS;R2-E 边界不被触发 |
| epi | SAS+SPSS | ✅ or_rr + attributable_risk | **零单元格路径(R2-C)不被触发** |
| standardization | SAS | ⚠️ 仅 direct 断言 | indirect/SMR 未断言 |
| diagnostic | SAS+SPSS | ✅ AUC | — |
| tableone | SAS+SPSS | ✅ means/sds/ns/pooled-t | — |

**未覆盖文件清单(零关联基线)**:空 ✅(12 模块每个至少有一处关联基线)。
**parity 盲区补基线优先级**:
1. **log-rank**(survival.ts,最复杂:矩阵求逆 + 协方差,零基线零引用)—— 最高危
2. **Kruskal-Wallis**(R2-D 恰落此盲区)
3. **Welch / paired / one-sample t**(生产函数全未被 oracle 断言,仅 pooled-t)
4. **Pearson χ²**(仅 Fisher 有 fixture)
5. **life_table / indirect standardization**(基线存在但未断言)

> 关键洞察:R2-A(ANOVA/相关/OLS)、R2-C(epi 零单元格)、R2-E(power 边界)、R2-B(回归族 se=0)均落在「退化/边界输入」,而现有基线用的都是良态数据集——**这些缺陷不会被当前 parity 套件捕获**。补退化用例与补上述基线是同一件事。

---

## 综合修复路线(含依赖与顺序)

### 阶段一:数值正确性(优先,医疗统计语境)
1. **R2-A + R2-B 一起改**(同一根因面):`distributions.ts` 两处 guard 拆极性 + 全回归族统一「se=0/非有限统计量」契约(统一 p→0 + `degenerate` 标记,或统一抛错)。这是唯一 High,先做。改后**必须**补退化用例(常数组 ANOVA / 完美相关 / 完美拟合 OLS / se=0 回归)并跑 `npm test` 验 PASS。
2. **R2-C**:epi 零单元格 0.5 校正或 not_computed;出口禁 NaN。局部改动,独立可验。
3. **R2-D + R2-E**:KW 结修正 + power 边界字段自洽。各自局部,互不依赖。
4. **R2-F**:`invertMatrixWithRidge` 加 `ridgeApplied` 标记并透传;属契约增强,配合 R2-B 的标记透传链一起做更省。

### 阶段二:前端
5. **R1**:StatsChartRenderer 三处 tooltip 文本字段加 `esc()`(推荐)或改 `renderMode: 'richText'`;去掉 test 里对 echarts-for-react 的 mock 或补一个真实渲染断言(含标记字符的列名/分组名 → 转义)。可与阶段一并行,无依赖。
6. **R1 上游核实**:读 `packages/server` dataset 解析,确认列名/分组值是否已清洗——据此决定 R1 端到端严重度是否降级。

### 阶段三:清理(无行为风险 / 低风险)
7. **R2-G**:logistic 空矩阵先抛契约错。1 行。
8. **R2 味道**:复用高精度 `inverseNormal` 替换 correlation 内嵌低精度 normQuantile;p 值统一走 clamp 的 `chiSquareP`;排序前过滤 NaN;迭代达上限时记标记而非静默。按性价比择优,不必一次做完。

### 补测试(与上面并行,单独排期)
- 退化输入用例:见阶段一各项「测试」小节。
- parity 基线补齐:log-rank → KW → Welch/paired t → Pearson χ² → life_table/indirect(按上表优先级)。

---

## 验证门要求(项目硬规则)

以下修复均属**行为变化**,按项目 CLAUDE.md 必须补「命令 → 关键输出 → PASS|FAIL|PARTIAL」才算完成:
- R2-A/B/C/D/E/F(改数值输出或错误契约)
- R1(改渲染转义)

只读代码 ≠ 验证。本文件为纯静态审查产物,**未运行测试套件**,不构成验证门通过。

---

## 残点队列(本轮之外,尚未审查)

- **R3**:research-workflow 审批门深挖(prompt 注入是否绕过审批、hash 复核完整性)——上一轮结尾 offer,未做。
- **R4**:replay 往返完整性深挖(导出成员齐全性、改一字节是否按设计失败、副作用隔离)——上一轮结尾 offer,未做。

> 上一轮已完成 11 项队列 + 安全汇总 + Top10(见另一份/上一会话记录);R1、R2 是对其中 item 10、item 7 两处「未核实/未深挖」标记的细化,结论分别为「item 10 侧栏清白但 tooltip 有口子」「item 7 结论推翻」。
