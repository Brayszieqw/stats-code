# Stats Code 只读工程审查 — R4（Snapshot/Replay 往返完整性）

> 日期：2026-07-14 · 模式：纯工程只读审查 · 主会话模型：claude-fable-5（取证亦由主审直读，executor 因 API 余额不可用）
> 范围：残点队列 R4 —— 导出成员齐全性、hash 自洽、改一字节是否按设计失败、副作用隔离。R3 已由用户确认关闭。
> 证据口径：所有 `path:line` 为实际读到的代码行。**本轮为纯静态审查，未运行测试套件**；修复后仍须补「命令→输出→PASS」验证门。

---

## 0. 一页速览（按修复优先级）

| # | 严重度 | 短名 | 位置 | 一句话 |
|---|--------|------|------|--------|
| R4-A | **High** | hash 先于 redact 计算 → 合法快照 replay 必失败 | `packages/server/src/providers.ts:201-205` + `packages/engine/src/snapshot/exporter.ts:235-237` | 步骤 hash 对原始字节算，包里存 redact 后字节；任一 JSON 成员含路径样式字符串即出「假篡改」 |
| R4-B | 中高 | 完整性门只锚一半成员，清单自身无锚 | `packages/engine/src/snapshot/replay.ts:136-206` | manifest 其余字段 / llm_provenance / narrative / coverage 改动不被察觉；workflow.yaml 可与成员同步改而全绿 |
| R4-C | Medium | replay 无生产入口 | `executeReplay` 全仓 grep：仅测试调用 | 无 CLI/路由/解包层；审计者拿到 zip 没有官方验证命令；副作用禁令无生效场景 |
| R4-D | Medium | 生产组装三个硬编码空值 | `packages/server/src/providers.ts:216-217,224` | `llmCalls:[]`（LLM 痕迹恒空）、`referenceSoftware:[]`（gate 2 恒过）、`apiKeys:[]`（密钥 redaction 恒关） |
| R4-E | Low | workflow.yaml 成员路径无边界校验 | `packages/engine/src/snapshot/replay.ts:76-85` + `workflow_yaml.ts:115-125` | `join(dir, path)` 直接拼接，parse 不查 `..` 组件；写侧有全套校验，读侧不对称 |
| R4-F | 味道 | 若干（见文末） | — | ZIP 无查重/无 ZIP64、manifest schema_version 不校验、commitSha 回落全 0、registry 无界不落盘、destination 任意路径 |

---

## R4-A 【确认缺陷 · High】步骤 hash 记录在 redact 之前，成员写入在 redact 之后 → 含路径文本的合法快照「出生即坏」

**根因链（三段，全部一手核实）**

1. `packages/server/src/providers.ts:201-205`：workflow.yaml 里记录的 input/output sha256 对 **原始** JSON 字节计算（`sha256Hex(protocolBytes/approvalBytes/auditBytes/resultBytes)`）；同一批原始字节在 `:210-215` 作为 artifacts 交给 exporter。
2. `packages/engine/src/snapshot/exporter.ts:235-237`：每个 artifact 写入 zip 前过 `redactTextBytes(art.bytes, policy)`。
3. `packages/engine/src/redact.ts:145-160`：**即使 policy 为空（secrets=[]、workingDirectory=undefined），`classifyPaths` 也无条件运行**——盘符路径 `[A-Za-z]:[\\/]…`（`redact.ts:83-94`）与 `/Users/ /home/ /root/ /private/ /tmp/ /var/` 前缀（`redact.ts:40`）一律替换为 `<external>`（workingDirectory 未定义时全部外部化，`redact.ts:109-119`）。

**现象**：只要 protocol.json（LLM/用户撰写的协议文本）、dataset-audit.json（含客户端提交的 `file_name`）、result.json 中任一出现绝对路径字符串（如 `C:\Users\x\cohort.csv`，JSON 转义形式同样命中），导出成功且无告警，但 replay 在 gate 3/4 报 `input_artifact_sha256_mismatch`——**对未被改动过的快照报「篡改」**。该错位在导出瞬间固化，事后无法补救，快照永久不可验证。

**何时出现**：上传时文件名带全路径、协议/结果文本引用本机路径 → 完成运行 → 导出 → 任何一次 replay。

**测试为何全绿（缺口=确认）**
- property 测试生成器字母表 `[a-z0-9":,{} .]` 生成不出路径样式（`tests/property/replay.property.test.ts:89-92`）。
- E2E 测试内容全程无路径字符（`tests/integration/research-snapshot.test.ts:57-84`）。
- `tests/integration/snapshot-writer.test.ts:151-175` 已证明 redaction 确实改写 artifact 字节（`C:/elsewhere/data.bin` → `<external>`），但该 fixture 从不接 replay——雷区的两半各在一个测试里，从未相遇。

**建议改法（最小）**：`providers.ts` 组装时**先 redact 再 hash**，把 redact 后字节交给 exporter——`redactPure` 有文档化幂等契约（`redact.ts:10-12`），exporter 的第二遍是 no-op，hash 与包内字节自然一致。补一个「artifact 含 `C:\x\y.csv` → 导出 → replay 通过」的回归用例。
**测试**：缺，如上。
**置信度**：高（代码可推证；`classifyPaths` 无条件运行为一手事实）。

---

## R4-B 【确认缺陷 · 中高】「改一字节应失败」只对约一半成员成立；校验清单自身无锚；整包锚不持久化

**hash 门实际覆盖面**（`packages/engine/src/snapshot/replay.ts`）
| 成员 | replay 消费 | 改动可察觉？ |
|------|-------------|--------------|
| data.csv | gate 1 对 manifest（`:146-155`） | ✅ |
| step inputs/outputs（protocol/approval/dataset-audit/result 4 件） | gate 3/4 对 workflow.yaml（`:195-204`） | ✅（前提：workflow.yaml 未同步改） |
| manifest.json | 只读 `input_dataset_sha256` 一个字段（`:105-107,136-144`） | ❌ 其余字段（run_id/created_at/release/commit/run_status）改动全绿 |
| versions.json | 只读 `reference_software`（`:108-110,157-182`） | ❌ runtime_dependencies/os 字段改动全绿 |
| llm_provenance.json / narrative.md / coverage.json | **不读不校验**（全文件零引用） | ❌ |
| workflow.yaml | 是校验清单，但**自身无外部锚** | ❌ 与成员同步改则全绿 |

**整包锚未保管**：导出返回 zip 整体 sha256（`providers.ts:121-122` → `router.ts:842-843`），但服务端不持久化（`createSnapshotRunRegistry` 的 `records` 只存运行注册，导出结果不回写会话）——锚的保管完全依赖调用方自觉。

**注释过度宣称**：`tests/property/replay.property.test.ts:3-5` 自述「for ALL single-byte tamperings of any contained artifact」，实际只篡改 data.csv 与一个 output artifact（`:101-139`）；`replay.ts:8-9` 自述 wave-1 范围倒是诚实的。

**建议改法**：manifest 升级为全成员 digest 清单（schema_version 2：manifest 携带每个成员的 sha256，replay 全量校验；manifest 自身 hash 即整包锚）；导出时把 zip sha256 回写会话记录，服务端留锚；property 测试改为对任意成员任意偏移采样篡改。
**测试**：缺 manifest 非数据字段 / llm_provenance / narrative / coverage / workflow.yaml 五类篡改用例。
**置信度**：高。

---

## R4-C 【确认缺陷 · Medium】replay 无生产入口；「往返」当前只是 hash 往返

- 全仓 grep `executeReplay`：定义（`replay.ts:118`）、导出（`snapshot/index.ts:62`）、三个测试文件——**无 CLI 子命令、无 HTTP 路由、无解包层**。生产侧连 zip 解包器都不存在（测试用 PowerShell `Expand-Archive`，顺带 Windows-only）。
- 副作用禁令（Req 7.5/7.6）是**自报告探针**：`plan.sideEffects` 由调用方传布尔（`replay.ts:37-42,121-133`），默认全 false 永不触发；本体倒确实纯读（fs read + hash + yaml parse，无 spawn/端口/锁代码）——「无误触端口/浏览器/锁」本轮结论为真，但靠的是实现纯净而非门。
- `stepsReplayed` 语义 = 通过 hash 门的步数；**不重新执行统计计算**（`replay.ts:7-9` 自述 wave-1，数值重算门被 artifact hash 门替代）。「replay 往返完整性」当前的真实保证：字节完整性往返，不含数值复现往返。
- 不对称：exporter 包了 `guardedSpawn`（`exporter.ts:146-148`），replay 没包——wave-2 引入步骤重跑时必须补上。

**建议改法**：加 CLI `replay <zip>` 子命令：解包（带条目路径校验）→ executeReplay → 人类可读报告；探针接真实检测（监听端口枚举/锁文件存在性）；文档明示 wave-1 语义。
**测试**：现有测试覆盖库函数层，入口层无从测起（不存在）。
**置信度**：高（零调用为 grep 事实）。

---

## R4-D 【确认缺陷 · Medium】生产组装硬编码空值：LLM 痕迹恒空、参考软件门恒过、密钥 redaction 恒关

- `packages/server/src/providers.ts:216-217,224`：`llmCalls: []`、`referenceSoftware: []`、`apiKeys: []`；`workingDirectory` 未传（undefined）。
- 后果一：`llm_provenance.json` 永远是 `{"schema_version":1,"calls":[]}`——研究工作流的协议编译/对话确实经过 LLM（orchestrator），快照却声称零调用，成员「齐全但恒空」，审计语义缺一块。
- 后果二：replay gate 2（参考软件）恒真。TS 后端本机计算无外部统计软件，可解释为有意，但应显式注释而非空数组暗示。
- 后果三：redaction 的 secret 半边在生产导出中未接线——若 API key 意外进入任何文本成员，不会被 `<redacted>`。engine 侧能力存在（`snapshot-writer.test.ts:151-175` 验证过），server 侧没接。
- 后果四：`workingDirectory` 未传 → R4-A 的路径替换全部走 `<external>`（最大信息损失路径），同时加宽 R4-A 触发面。

**建议改法**：`llmCalls` 从 orchestrator 的调用记录取（llm_provenance.ts 的 hash-only 结构已备好）；`apiKeys` 从 LLM 配置 store 取；`workingDirectory` 传数据目录根；`referenceSoftware` 留空则加注释声明设计意图。
**测试**：缺「快照内 llm_provenance 与会话 LLM 调用数一致」断言。
**置信度**：高。

---

## R4-E 【确认缺陷 · Low】成员路径读侧无边界校验（写侧有，不对称）

- 读侧：`replay.ts:76-85` `readMember` 用 `join(dir, archivePath)` 直接拼接；路径来自 workflow.yaml 的 `input.path/output.path`，而 `workflow_yaml.ts:115-125` `parseArtifactRef` 只验 path 是字符串、sha256 是 64 位 hex——**不查 `..` 组件/绝对形式**。被改过的 workflow.yaml 可令 replay 读快照目录之外的文件做 hash 比对。
- 写侧对照：`zip_writer.ts:38-50` `validateEntryName` 有全套（空名/`/`开头/反斜杠/NUL/`..`/长度）。
- 影响温和（只读 + 比对，需本地文件配合），一行修复：`parseArtifactRef` 复用写侧规则。

**置信度**：高。

---

## R4-F 味道与观察（简列）

- **ZIP 无 ZIP64、无查重**：`zip_writer.ts:75-152` u16/u32 字段，data.csv 不受 50MB 上限约束（上限只算 artifacts，`exporter.ts:160-167`），>4GB 会写坏包先 OOM（逐字节 `number[]` push，~8 字节/byte 内存放大）；`buildZipBytes` 不查重复条目名，artifacts 路径与 7 个固定成员重名时包内出双条目（今日组装用固定名不撞，无守卫防将来）。
- **manifest/versions 的 schema_version replay 不校验**（`replay.ts:105-110` 只取单字段）；workflow 有校验（`workflow_yaml.ts:222-230`）。格式演进时旧 replay 静默接受新 manifest。
- **commitSha 回落全 0**：`providers.ts:138-141` 环境变量缺失时 40 个 `0`，溯源字段成占位无告警。
- **snapshot registry 内存态**：`providers.ts:149-156` `records` Map 无上限、不落盘，重启后导出报 `run not found` → 500 `InternalError`（`router.ts:844-846`），错误可理解性差。
- **destination 客户端任意路径**：`contract/sidecar.ts:67-70` + `router.ts:842`，服务端按客户端字符串写 zip。本地单用户设计可解释；与上一轮已记录的 CORS `*` 叠加时应收敛（限定目录或改流式下载）。
- **测试用 PowerShell Expand-Archive**（replay/snapshot/E2E 三处）——测试套件 Windows-only 假设，跨平台 CI 会挂（属 item 3/9 交界，此处仅记）。

**良好实践（修复勿破坏）**
- 确定性 ZIP：Stored、固定 mtime、按名字节序、字节稳定——有双重测试锚（`snapshot-writer.test.ts:68-93`、`snapshot.property.test.ts:73-108`）。
- 原子写：`.tmp`+fsync+rename，任何失败零残留——Property 18 四用例（`snapshot-writer.test.ts:178-231`）。
- data.csv verbatim 保证 hash 恒一致（`exporter.ts:225-227`）。
- narrative 引用先全量验证后产出（`narrative.ts:49-55`）。
- exporter 包 `guardedSpawn`，统计运行时黑名单齐全（`spawn_policy.ts:20-43`），嵌套 ref-count、异常恢复正确（`:202-228`）。
- workflow.yaml 双向往返契约 + 关闭错误规则集（`workflow_yaml.ts:4-14,68-90`）。

---

## 覆盖率自检（8 条死扣检查单）

| # | 检查单 | 结论落点 |
|---|--------|----------|
| 1 | 成员齐全性 | 8 成员核实；llm_provenance 恒空 → R4-D |
| 2 | hash 自洽 | 顺序断裂 → R4-A；覆盖面 → R4-B |
| 3 | 改一字节按设计失败 | data.csv/artifacts ✅ 有测试；其余成员无门 → R4-B |
| 4 | 往返比对口径 | wave-1 = hash 往返，无数值重算 → R4-C |
| 5 | 副作用隔离 | 本体纯读 ✅；门为自报告、无生产调用方 → R4-C |
| 6 | zip 写入器契约 | 确定性 ✅；查重/ZIP64 缺 → R4-F |
| 7 | 版本契约 | workflow ✅；manifest/versions 不查 → R4-F |
| 8 | 测试覆盖 | 篡改仅 2 类成员；property 注释过度宣称；生成器避开触发字符 → R4-A/B |

无跳项。

## 修复顺序建议

1. **R4-A + R4-D 同批**（同在 providers.ts 组装函数）：先 redact 再 hash（靠幂等契约，改动约 10 行）+ 接线 apiKeys/llmCalls/workingDirectory；补「含路径字符 artifact 往返」回归用例。改后跑 replay/snapshot 全族测试验 PASS。
2. **R4-B**：manifest schema_version 2 全成员 digest + zip sha 回写会话；property 测试改任意成员采样。
3. **R4-C**：CLI replay 子命令 + 解包器（含条目校验）+ 真探针。
4. **R4-E** 一行校验；**R4-F** 按性价比择优。

## 验证门要求（项目硬规则）

R4-A/B/C/D/E 均为行为变化，修复必须附「命令 → 关键输出 → PASS|FAIL|PARTIAL」。本文件为纯静态审查产物，不构成验证门通过。

---

## 残点队列状态

- **R3**（research-workflow 审批门）：用户确认关闭（2026-07-14）。本轮主审直读补充记录：三重复核结构（批准时重算审计逐字段比对 `research-workflow.ts:212-229`、执行时 spec/dataset/audit 三 hash 复核 `:295-347`、终线性化复核 `:359-375`）+ canonical JSON 同源 hash（`research-integrity.ts:16-62`）+ 保留键深度拒绝（`:65-80`），骨架扎实。
- **R4**（replay 往返完整性）：**本文件，已完成**。
- 队列清空。
