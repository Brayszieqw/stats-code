# Stats Code 只读工程审查 — R5（审批链地基与旁路：协议审批语义 / store 落盘完整性 / 执行入口）

> 日期：2026-07-14 · 模式：纯工程只读审查 · 主审：claude-fable-5（R5.1 直读 + 要害抽查）· 取证：executor(claude-sonnet-5)
> 范围：R4 报告「残余盲区」第 1+2+3 条合并（协议审批语义、store 完整性、orchestrator 旁路）+ gate 测试覆盖面。
> 证据口径：path:line 为一手代码行（主审直读或 executor 取证后主审抽查）。**本轮为纯静态审查，未运行测试套件。**

---

## 0. 一页速览

**总评：正面结论为主的一轮。** 三大盲区假设——「PATCH 可客户端直通审批」「加载时盲信存储字段」「orchestrator 存在旁路」——**全部被证伪**。真实缺陷集中在可观测性与测试覆盖，无 High。

| # | 严重度 | 短名 | 位置 | 一句话 |
|---|--------|------|------|--------|
| R5-A | Medium | 加载期完整性降级/过滤**静默**，且下次保存即永久抹除 | `file-session-store.ts:148-204,243-249` | 失配的协议降级 Draft、审计/审批记录被剔除——无日志、无隔离备份，法证信号随下次 save 销毁 |
| R5-B | Medium | 生产持久化路径零集成测试 | `packages/api/src/launcher.ts:67` vs 全部测试用 MemSessionStore | R5-A 的降级逻辑从未被任何测试触发；mem/file 守卫逐字相同但无契约测试防漂移 |
| R5-C | Low | gate 测试缺两类用例 | `research-workflow-gate.test.ts` | 「数据集变更后拒绝」「同 plan 重复执行」无用例（前者门禁代码存在，后者为无消费标记的设计语义待锁定） |
| R5-D | Info（正面） | 执行入口结构性无旁路 | 见 §4 | orchestrator 依赖注入中根本没有 runner 引用；skillRunner.run 全仓唯一调用点在 execute 全部门禁之后 |
| R5-E | Info | 双 approve 无去重 | `mem-store.ts:86-100` / `file-session-store.ts:334-348` | 重复 approval 记录仅冗余不破坏完整性（绑定同一批 hash） |

---

## 1. R5.1 协议审批语义（主审直读）——地基成立

**结论：「客户端可 PATCH status:'Approved'」= 本人一键审批，不是漏洞。** 本地单用户信任模型下审批人=键盘前的用户；服务端保住了所有该由服务端保住的东西：

- **approval_id 服务端签发**：`router.ts:367-369` randomUUID，客户端不可提交（schema `.strict()` 多余键直接 422，`domain.ts:227-233`）。
- **content_sha256 服务端重算**：`router.ts:350`，非客户端提交值。
- **版本 CAS**：更新已有协议必须带 `expected_version`（`router.ts:335-348`）；任何语义变迁必进位版本（`:353-356`，注释明说防「陈旧请求撤销后复活审批」）。
- **内容变更 ⇒ 新审批**：`preserveApproval` 仅在内容 hash 相同且已 Approved 时保留旧 id（`router.ts:358-361`），改内容必然换 approval_id → 旧 plan 级审批随之失效（gate 测试 10c 直证）。
- **审批完整性在 schema 层**：Approved 时 7 个必填字段非空由 `superRefine` 强制（`domain.ts:234-244`）。
- **LLM 编译与审批严格隔离**：compile 路由只提案永不落盘（`router.ts:287-304`）；proposal schema `.strict()` 仅 15 内容字段，「model output may never smuggle approval metadata」（`domain.ts:266-267`）；`approval_required: z.literal(true)`（`:277`）；编译器系统提示禁止返回审批字段、输出 32k 上限、15 字段严格校验（`protocol-compiler.ts:13-27,63-65,107-110`）。
- **测试直证**：gate 用例 10a——PATCH 至 Approved 后立即 run 仍 428 ResearchApprovalRequired（协议审批 ≠ 可执行，还需数据审计+plan 审批）。

设计注记（非缺陷）：无双人复核角色分离；放大器为上一轮已记录的 CORS `*`（跨源页面可调本地 API），修 CORS 即收敛，此处不重开。

---

## 2. R5-A 【确认缺陷 · Medium】加载期完整性处理：方向正确（fail-closed），但静默 + 复写即抹除

**先记正面**：加载校验比预期强——不是盲信存储字段：
- 协议：schema 解析后**重算** `protocolContentSha256` 与 `protocolStateSha256` 双比对，外加「Approved ⇒ approval_id 非空」显式检查（`file-session-store.ts:150-156`）；`protocolStateSha256` 覆盖 version/status/approval_id/approved_at（`research-integrity.ts:98-101`），篡改任一元数据字段都会失配（主审抽查证实）。
- 审计记录：重算 `sha256Canonical(content)` 对 `audit_sha256`（`:170-184`）。
- 审批记录：整链重推——协议三元组、dataset sha、audit 存在性+hash、`runSpecSha256` 重算（`:185-204`）。
- legacy 迁移永不铸造信任：旧 Approved 一律降 Draft、approval_id 置 null（`:120-146`，注释「never mint trust for them」）。

**缺陷本体**：
- 失配后**静默**降级/过滤：全文件无任何 console.* / 告警字段（executor grep 证实）；协议悄悄变 Draft、记录悄悄消失，用户与审计者均无从知晓发生过什么。
- **不对称**：整文件损坏有 `.corrupt-<stamp>` 改名备份（`:220-227,237-240`）；record 级失配**无任何隔离**——`ensureLoaded` 一次载入内存（`:229-241`），下一次任何变更触发 `save()` 整文件重写（`:243-249`），被过滤的原始记录**永久销毁**。篡改/损坏的法证信号恰恰在最需要留存的场景被抹掉。
- **何时出现**：会话文件任何字节级损坏（磁盘/同步盘/手工编辑/未来代码 bug 写坏字段）→ 重启加载 → 「我批过的协议怎么变回草稿了」且无解释。
- **建议改法**：失配时 (a) 记结构化告警（日志 + 会话上加 `integrity_warnings` 字段透传前端）；(b) 复用 backupCorrupt 模式把被剔除记录写 `.quarantine-<stamp>`；(c) 集成用例「写入→篡改 approval_id→重载→断言降级+告警产生」。
- **测试**：零（见 R5-B）。置信度：高（主审直读）。

顺带味道：`save()` 用 tmp+rename 但无 fsync（对照 snapshot zip 写入器有 fsync，`zip_writer.ts:176-183`）——崩溃窗口不一致；mode 0o600 是好实践。

## 3. R5-B 【确认缺陷 · Medium】生产 store 路径零集成测试；双实现无契约锁

- 生产装配用 `createFileSessionStore()`：`packages/api/src/launcher.ts:67`（launcher 即 stats-code.exe 实际入口）。
- 全部 router/workflow 集成测试用 `MemSessionStore`（含 `research-workflow-gate.test.ts:38,239`）；`createFileSessionStore` 仅一个不经 buildRouter 的单测（`tests/unit/file-session-reuse.test.ts`）。
- 后果一：R5-A 的全部加载校验/降级逻辑**从未被任何测试执行**。
- 后果二：mem/file 两实现的 append 守卫今日逐字相同（`mem-store.ts:90-96` ↔ `file-session-store.ts:337-343`，仅差深拷贝+落盘），但无共享实现或双实现契约测试——将来单边改动无警报。
- 建议：SessionStore 契约测试套件（同一批用例参数化跑两实现）+ file store 的篡改-重载用例。置信度：高。

## 4. R5-D 【Info · 正面】执行入口盘点：结构性无旁路

| 入口 | 门 | 证据 |
|------|----|------|
| POST …/datasets/:did/audit | researchWorkflow.auditDataset（内含协议门） | `router.ts:629-661` |
| POST …/analysis-plans/approve | researchWorkflow.approveAnalysisPlan | `router.ts:663-698` |
| POST …/run | researchWorkflow.execute；请求 schema `.strict()`，仅 plan_id 可选，**无** allow_matching_plan 字段 | `router.ts:700-759`，`domain.ts:88-94` |
| 对话路径（messages → orchestrator） | run_skill 分支 → `researchWorkflow.execute({allowMatchingPlan:true})`，完全依赖 execute 内部门禁 | `orchestrator.ts:505-516`（经 `router.ts:401`） |

- **结构性保证**：OrchestratorDeps 注入清单里没有 SkillRunner（`orchestrator.ts:28-34`）——对话路径想绕门在代码结构上不可达；`skillRunner.run` 全仓唯一调用点 = `research-workflow.ts:379`，位于全部门禁（`:272-376`）之后；router 从不直接引用 skillRunner/skillRegistry（executor 全仓 grep）。
- `skill-runner.ts` 零审批逻辑（grep "approv|protocol" 0 命中）；`:220` 是逻辑回归分离诊断的统计学 warning，与审批无关——R2 报告引用语境相符。
- `allowMatchingPlan:true` 仅 orchestrator 内部一处赋值；HTTP 层 strict schema 使客户端无法设置。语义：对话代理只能重跑「人已批准过的完全相同 spec」（run_spec_sha256 倒序精确匹配，`research-workflow.ts:283-287`）。

## 5. R5-C 【测试缺口 · Low】与 R5-E 【Info】

- gate 测试 5 用例质量高（含并发窗口用例 10d：audit 重算期间协议被改 → 原子拒绝、approvals 保持空）。缺：
  1. 「数据集变更后拒绝」——门禁代码在 `research-workflow.ts:310-319`（R3 轮主审直读核实），本文件无用例（是否在 dataset-upload/run-endpoint 测试覆盖未查，列残余）；
  2. 「同 plan_id + 同 args 重复执行」——execute 无 approval 消费/单次标记（executor 逐行核实 `:272-434`），同一审批可反复执行。幂等重算 + 每次留 run 记录，倾向视为设计；建议补一条语义锁定用例，防止将来无意改变。
- R5-E：append 无去重，两次满足条件的 approve 各留一条记录（mem/file 同）。同 spec 重复审批绑定同一批 hash，无完整性破坏，仅记录冗余。观察级。

---

## 覆盖率自检（12 条死扣检查单）

| 条目 | 落点 |
|------|------|
| ①-④ 协议审批语义 | §1 主审直读闭合（含 schema strict 性、preserveApproval、compile 隔离） |
| ⑤ append CAS 条件 | executor 表1：五条件比较（status/version/content_sha256/approval_id/会话 Active），主审抽查一致 |
| ⑥ state_sha256 存/取/失败行为 | §2：存时调用方算、取时重算双 hash、失败静默降级（executor 表3 + 主审抽查 `:148-168`） |
| ⑦ mem/file 契约一致性 | §3：今日逐字相同、无契约锁 |
| ⑧ 加载后三元组信任 | §2 正面：重算不盲信；version 由 state_sha256 间接覆盖 |
| ⑨-⑪ 入口/直调/allowMatchingPlan | §4 闭合 |
| ⑫ gate 测试逐用例 | executor 表10 + §5 |

无跳项。**残余未知**：(a) 数据集变更拒绝是否在其他测试文件有用例；(b) `launcher.ts` 100 行之后未读；(c) 真并发双 approve 的 loadAuditContext 竞态未深挖（已定 Info，10d 已覆盖更关键的 protocol-change 竞态）。

## 修复顺序建议（均无依赖，可并行）

1. **R5-A**：告警 + quarantine（复用现成 backupCorrupt 模式，改动集中一个文件）。
2. **R5-B**：SessionStore 契约测试套件 + 篡改-重载集成用例（顺手补 save() 的 fsync）。
3. **R5-C**：两条用例。

## 验证门要求

R5-A 修复属行为变化（新增告警/隔离文件），R5-B/C 为测试新增——完成时须附「命令 → 关键输出 → PASS」。本文件为静态审查产物。

---

## 残点队列状态（2026-07-14）

- R1 前端 tooltip / R2 数值内核：已完成（见 2026-07-13 报告）；**R2 修复至今无验证门记录（挂账）**。
- R3 审批门：用户关闭；其三条残留悬点已并入本轮全部闭合（结论偏正面）。
- R4 replay 往返：已完成（同日报告，R4-A High 待修）。
- **R5 审批链地基与旁路：本文件，已完成。**
- 剩余盲区（未开）：dataset-audit 规则内核 · sidecar 等价代码生成器 · R1 上游 ingest 清洗核实 · engine CLI 命令面 · 安全专项三题是否已做需用户确认。
